# Django Security
# Generar una nueva clave con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=change-me-to-a-secure-random-key
DEBUG=True

# Database Configuration (Docker defaults)
DB_NAME=skating_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432